function solution(s) {
    var answer = '';

    const number = s.split(' ').map((item) => parseInt(item));
    number.sort((a, b) => a - b);
    answer += number[0] + ' ';
    answer += number[number.length - 1];


    return answer;
}
